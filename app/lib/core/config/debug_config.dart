/// Debug flags for development and screenshots
/// Set these to true when needed, but NEVER commit with true values!
class DebugConfig {
  /// Force show onboarding even if already completed
  static const bool forceOnboarding = false;
  
  /// Force premium status (bypass purchase check)
  static const bool fakePremium = false;
  
  /// Skip onboarding entirely (go straight to home)
  static const bool skipOnboarding = false;
}
